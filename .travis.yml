language: python

sudo: required

python:
  - "2.7_with_system_site_packages"

cache: pip

os: Linux

script: 
  - pytest

install:
  - python installer.py
  - pip install requests
  - pip install prettytable
  - pip install termcolor
  - cd tests

